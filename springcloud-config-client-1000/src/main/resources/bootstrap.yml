spring:
  cloud:
    config:
      name: application-config-client  # 需要从github上读取的资源名称，注意没有yml后缀名
      profile: test  # 本次访问的配置项
      label: master
      uri: http://localhost:2000  #
#      uri: https://github.com/nameyou/spingconfig-demo.git
